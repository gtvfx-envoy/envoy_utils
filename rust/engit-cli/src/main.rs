//! Native `engit` CLI entry point.
//!
//! Placeholder for the Phase 5 port of `py/engit/_cli.py`. Unlike `envoy`,
//! `engit` has no Python library API -- this binary is the only distributed
//! artifact, replacing `dist/engit.exe`.

fn main() {
    println!("engit-cli: scaffolding placeholder (Phase 5 will port py/engit/_cli.py here)");
    std::process::exit(0);
}
